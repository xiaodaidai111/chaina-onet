# chaina-onet

This repository contains the uploadable project files from `D:\创业\职位招聘——蒋`.

The following local files are intentionally not tracked because they exceed GitHub's normal file-size limits:

- `_src.zip`
- `_src/models/pytorch_model.bin`
- `_src/models/onnx/model.onnx_data`

